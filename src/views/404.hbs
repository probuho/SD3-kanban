<main class="container p-5">
  <div class="row">
    <div class="col-md-4 offset-md-4">
      <div class="card card-body">
        <h1>{{__ 'not_found'}}</h1>
        <p>{{__ 'page_not_exist'}}</p>
        <a href="/" class="btn btn-primary">{{__ 'return_home'}}</a>
      </div>
    </div>
  </div>
</main>